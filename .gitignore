node_modules
dist
.DS_Store
*.local
.vite

# Source video is large; keep it out of version control if you prefer.
# Comment this out if you want the video tracked.
*.mp4
*.mov
*.webm
*.mkv

# ...but the compressed loader video must ship with the site
!public/tyc-logo.mp4

# Original photos/videos stay local-only — the site serves the
# web-optimized copies from public/media instead.
public/Website Resimleri ve Videoları/
!public/media/**/*.mp4
